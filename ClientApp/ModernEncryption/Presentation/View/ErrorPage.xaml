<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ModernEncryption.Presentation.View.ErrorPage"
             Title="{Binding Title}">
    <StackLayout>

        <Button Text="zurück" HorizontalOptions="Start" VerticalOptions="Start"  BackgroundColor="White"  BorderColor="Gray" Command="{Binding BackToPreviousPageCommand}"></Button>

        <Label Text="Der passende Fehler"/>
    </StackLayout>
</ContentPage>