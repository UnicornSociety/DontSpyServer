<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ModernEncryption.Presentation.View.RegistrationPage"
             xmlns:local="clr-namespace:ModernEncryption.Utils;assembly=ModernEncryption"
             xmlns:i18n="clr-namespace:ModernEncryption.Presentation.View;assembly=ModernEncryption"
             xmlns:behavior="clr-namespace:ModernEncryption.Presentation.Behaviors"
             xmlns:converter="clr-namespace:ModernEncryption.Presentation.Converter"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:FirstValidationErrorConverter x:Key="FirstValidationErrorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="{i18n:Translate WelcomeTo}" FontSize="45" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                    <Image Source="{local:ImageResource ModernEncryption.logo.png}" HeightRequest ="300" HorizontalOptions="Center"  VerticalOptions="Center" />
                    <!-- Logo ist nur Platzhalter-->
                    <Label Text="l" FontSize="10" TextColor="White"></Label>
                    <Label Text="{i18n:Translate RegisterRequest}" FontSize="20"></Label>
                    <Entry Text="{Binding Displayname.Value, Mode=TwoWay}" Placeholder="{i18n:Translate Displayname}">
                        <Entry.Style>
                            <OnPlatform x:TypeArguments="Style"  
                                        iOS="{StaticResource EntryStyle}"  
                                        Android="{StaticResource EntryStyle}"  
                                        WinPhone="{StaticResource EntryStyle}"/>
                        </Entry.Style>
                        <Entry.Behaviors>
                            <behavior:EventToCommandBehavior EventName="TextChanged" Command="{Binding ValidateDisplaynameCommand}" />
                        </Entry.Behaviors>
                        <Entry.Triggers>
                            <DataTrigger TargetType="Entry" Binding="{Binding Displayname.IsValid}" Value="False">
                                <Setter Property="behavior:LineColorBehavior.LineColor" Value="{StaticResource ErrorColor}" />
                            </DataTrigger>
                        </Entry.Triggers>
                    </Entry>
                    <Entry Text="{Binding Email.Value, Mode=TwoWay}" Placeholder="{i18n:Translate Email}">
                        <Entry.Style>
                            <OnPlatform x:TypeArguments="Style"  
                                        iOS="{StaticResource EntryStyle}"  
                                        Android="{StaticResource EntryStyle}"  
                                        WinPhone="{StaticResource EntryStyle}"/>
                        </Entry.Style>
                        <Entry.Behaviors>
                            <behavior:EventToCommandBehavior EventName="TextChanged" Command="{Binding ValidateEmailCommand}" />
                        </Entry.Behaviors>
                        <Entry.Triggers>
                            <DataTrigger TargetType="Entry" Binding="{Binding Email.IsValid}" Value="False">
                                <Setter Property="behavior:LineColorBehavior.LineColor" Value="{StaticResource ErrorColor}" />
                            </DataTrigger>
                        </Entry.Triggers>
                    </Entry>

                    <Button Text="{i18n:Translate SendPinToEmailCallToAction}" TextColor="White"  BorderRadius="15" BackgroundColor="#3A99D8" Command="{Binding SendVoucherCommand}" />
                    <Label Text="{Binding Displayname.Errors, Converter={StaticResource FirstValidationErrorConverter}" Style="{StaticResource ValidationErrorLabelStyle}" />
                    <Label Text="{Binding Email.Errors, Converter={StaticResource FirstValidationErrorConverter}" Style="{StaticResource ValidationErrorLabelStyle}" />
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>